import { Component } from "react";
import { ReactDOM } from "react-dom";

/*const Fruit = (props) => {
    return (
        <div>
            <h1>Meyvelerin Listesi</h1>
            <p>İsim : {props.fruit.name}</p>
            <p>Renk : {props.fruit.color}</p>
        </div>
    )
}

class Car extends Component {
    constructor() {
        super()
        this.state() = {
            car: "Ferrari"
        }
    }

    changeMessage() {
        this.setState({
            car: "Jaguar"
        })
    }

    render() {
        <div className="App">
            <h1>{this.state.car}</h1>
            <button onClick={() => this.changeMessage()}>Değiştir</button>
        </div>
    }
}

function App() {

    const fruits = {
        name: "mango",
        color: "kahverengi"
    }

    return (
        <div>
            <Fruit fruits={fruits} />
            <hr></hr>
            <Car />
        </div>
    )
}

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
export default Fruit*/